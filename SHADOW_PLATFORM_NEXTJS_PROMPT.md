# Shadow Platform — Build Prompt

انسخ كل شي تحت هذا السطر في جلسة وكيل جديدة منفصلة عن التطبيق.

---

```
أنت مهندس Full-Stack خبير في Next.js وTypeScript وPostgreSQL. ستبني
منصة "شادو" (Shadow) من الصفر — بوابة ويب تدير خدمات دعم الطلاب ذوي
الإعاقة داخل الجامعات السعودية.

هذا مشروع جديد في مجلد فارغ: D:\Shadow\platform
لا علاقة له بمشروع التطبيق (D:\Shadow\app). لا تلمس التطبيق.

═══════════════════════════════════════════════════════════════
ماذا تفعل المنصة
═══════════════════════════════════════════════════════════════

رحلة الطالب عبر المنصة:

1. الطالب يسجّل، يملأ بياناته، يرفع وثائقه الطبية (PDF مشفّر)
2. المختص يستلم الطلب، يراجع الوثائق، يصنّف الطالب ويحدد مستوى دعمه
3. المختص ينشئ خطة دعم تحدد الأدوات المفعّلة في التطبيق
4. بعد الاعتماد، التطبيق يقرأ الخطة عبر API ويتكيّف تلقائياً
5. المنصة تعرض بيانات الاستخدام وتنبيهات المرشد والتقارير

═══════════════════════════════════════════════════════════════
الأدوار والصلاحيات (RBAC — من اليوم الأول)
═══════════════════════════════════════════════════════════════

■ طالب (Student):
  - يسجّل، يرفع وثائق، يرى حالة طلبه
  - يرى الأدوات المفعّلة في خطته (بدون تفاصيل التصنيف أو المستوى)
  - لا يرى أبداً: تصنيفه، مستوى دعمه، ملاحظات المختص، التقارير الطبية

■ عضو هيئة تدريس (Faculty):
  - يرى طلاب مقرراته فقط
  - يرى التسهيلات المعتمدة (وقت إضافي، إلخ)
  - لا يرى: التقارير الطبية، التشخيص، مستوى الدعم

■ مختص / مرشد (Specialist):
  - يرى قائمة الطلاب المحالين إليه
  - يفتح الوثائق الطبية
  - يصنّف الطالب ويحدد المستوى ويعتمد خطة الدعم
  - يرى بيانات الاستخدام والتنبيهات
  - يعدّل المستوى لاحقاً

■ مدير / إدارة (Admin):
  - كل ما سبق + إدارة المستخدمين والأدوار
  - إحصائيات على مستوى الجامعة
  - إدارة الجامعات (multi-tenant)

═══════════════════════════════════════════════════════════════
نظام التصنيف ومستويات الدعم (Seed Data)
═══════════════════════════════════════════════════════════════

خمسة تصنيفات عامة:

1. اضطرابات النمو العصبي
   → التوحد، فرط الحركة وتشتت الانتباه
2. صعوبات التعلم
   → صعوبات القراءة، الكتابة، الحساب، الفهم الأكاديمي
3. الإعاقات الإدراكية الخفيفة
   → الإعاقة الذهنية البسيطة
4. اضطرابات التواصل واللغة
   → صعوبات النطق، الفهم اللغوي، التعبير
5. الاضطرابات السلوكية والانفعالية
   → مشكلات السلوك، الاندفاع، القلق داخل البيئة التعليمية

ثلاثة مستويات دعم:
- دعم خفيف: شبه مستقل، تذكيرات محدودة
- دعم متوسط: متابعة ومساعدة في بعض المواقف
- دعم مكثف: دعم مستمر وتدخّل بشري عند الحاجة

المستوى يُحدَّد بالاحتياج الفعلي لا بالتشخيص. والهدف خفضه تدريجياً.

═══════════════════════════════════════════════════════════════
التقنيات — قرارات نهائية
═══════════════════════════════════════════════════════════════

| الطبقة | الاختيار |
|---|---|
| Framework | Next.js 15 (App Router) + TypeScript strict |
| قاعدة البيانات | PostgreSQL |
| ORM | Prisma |
| المصادقة | NextAuth.js (Credentials provider + JWT) |
| تخزين الملفات | S3-compatible (MinIO محلياً للتطوير) |
| تنسيق CSS | Tailwind CSS |
| مكتبة المكونات | shadcn/ui |
| التحقق | Zod |
| الاستضافة لاحقاً | داخل السعودية (PDPL) |

لا تستخدم: Firebase, MongoDB, GraphQL, أي CMS جاهز.
لا تُضِف مكتبة بلا إذن — اقترح واسأل أول.

═══════════════════════════════════════════════════════════════
الكيانات المطلوبة في قاعدة البيانات
═══════════════════════════════════════════════════════════════

صمّم schema يشمل:

• Tenant (الجامعة) — كل شيء تحته tenant_id
• User — مع role (student/faculty/specialist/admin)
• StudentProfile — تخصص، مرحلة، معلومات تواصل
• Category (مرجعية مُبذورة) — الخمسة تصنيفات
• Condition (مرجعية مُبذورة) — الحالات الفرعية تحت كل تصنيف
• SupportLevel (مرجعية مُبذورة) — الثلاثة مستويات
• Document — مرجع للملف المشفّر + metadata (لا binary في DB)
• Assessment — ملاحظات المختص، التصنيف المُعتمد، المستوى، التاريخ
• SupportPlan — الأدوات المفعّلة، الحالة (مسودة/معتمدة/منتهية)
• PlanRevision — تاريخ التعديل، من عدّل، السبب، المستوى الجديد
• UsageEvent — يرد من التطبيق: أي وضع استُخدم، كم مرة، متى
• MentorAlert — تنبيهات مولّدة من أحداث الاستخدام
• AuditLog — كل وصول لسجل طالب: من، متى، ماذا

═══════════════════════════════════════════════════════════════
API للتطبيق (Flutter ↔ المنصة)
═══════════════════════════════════════════════════════════════

التطبيق يستدعي هذه النقاط:

POST /api/auth/login → JWT token
POST /api/auth/refresh
GET  /api/student/profile → { category, supportLevel, enabledTools }
GET  /api/student/support-plan
POST /api/events → batch usage events
POST /api/documents/upload → encrypted upload (specialist only reads)
GET  /api/documents/:id → download (specialist only)

كل endpoint يتحقق من: المصادقة + الدور + الـ tenant. الطالب لا يرى
بيانات غيره. المختص لا يرى طلاب خارج جامعته.

═══════════════════════════════════════════════════════════════
الأمان (PDPL — غير قابل للتأجيل)
═══════════════════════════════════════════════════════════════

- تشفير الوثائق at rest + in transit
- لا PII في logs أبداً
- كل وصول لسجل طالب → audit log
- صلاحيات مفروضة في middleware + Prisma policies
- tenant isolation عبر global middleware (كل query مقيّد بـ tenant_id)
- حق الطالب في حذف بياناته (soft delete + scheduled purge)

═══════════════════════════════════════════════════════════════
التعددية المؤسسية (Multi-Tenancy)
═══════════════════════════════════════════════════════════════

- كل جامعة = tenant
- كل جدول فيه tenant_id
- middleware يحقن tenant_id تلقائياً في كل query
- مستحيل مستخدم جامعة A يرى بيانات جامعة B

═══════════════════════════════════════════════════════════════
الهوية البصرية
═══════════════════════════════════════════════════════════════

نفس هوية التطبيق — متناسقين كمنتج واحد:

- كحلي: #1E2A3A (sidebar, headers, primary)
- كريمي: #ECE7DC / #F5F1E9 (backgrounds)
- برتقالي: #B5623A (CTA الرئيسي فقط — لمسة واحدة)
- خط: Tajawal أو IBM Plex Sans Arabic
- RTL بالكامل — العربية أولاً
- shadcn/ui مع تخصيص الألوان أعلاه

═══════════════════════════════════════════════════════════════
هيكل المشروع
═══════════════════════════════════════════════════════════════

D:\Shadow\
├── app\          ← التطبيق Flutter (لا تلمسه)
├── platform\     ← هذا المشروع (Next.js)
│   ├── prisma/
│   │   ├── schema.prisma
│   │   └── seed.ts
│   ├── src/
│   │   ├── app/           (App Router pages)
│   │   ├── components/    (shadcn + custom)
│   │   ├── lib/           (db, auth, utils, constants)
│   │   ├── middleware.ts  (auth + tenant + audit)
│   │   └── types/
│   ├── .env.local         (git-ignored — DB URL, secrets)
│   ├── .env.example       (committed template)
│   └── package.json
└── docs\         ← وثائق مشتركة

═══════════════════════════════════════════════════════════════
ما لا تفعله
═══════════════════════════════════════════════════════════════

- لا تلمس D:\Shadow\app — التطبيق مُنجَز ومنفصل
- لا Firebase
- لا AI/chatbot داخل المنصة
- لا مكتبات بلا إذن
- لا تخزّن ملفات طبية في DB — object storage + مرجع فقط
- لا ترسل بيانات طلاب لأي API خارجي من المنصة
- لا تبنِ ميزات غير مذكورة — اسأل أول

═══════════════════════════════════════════════════════════════
كيف تعمل معي
═══════════════════════════════════════════════════════════════

- خطّط قبل الكود لأي شيء يمس البيانات أو الصلاحيات
- Commit واحد لكل وحدة منطقية (Conventional Commits)
- اسأل قبل أي package جديد
- اشرح قراراتك بلغة واضحة
- اختبر الصلاحيات وعزل البيانات — هذه ليست اختيارية
- لا تترك المشروع مكسوراً — أصلح أو ارجع قبل ما تنتهي

═══════════════════════════════════════════════════════════════
المراحل — ثلاث فقط
═══════════════════════════════════════════════════════════════

المرحلة 0 — التخطيط (هذه الجلسة، بدون كود):

أعطني:
1. إعادة صياغة بعشر أسطر — أثبت أنك فهمت. اذكر كل غموض.
2. ERD بـ Mermaid — كل الكيانات أعلاه مع العلاقات.
3. جدول صلاحيات: لكل دور × كل كيان = ماذا يستطيع/لا يستطيع.
4. تصميم رفع الوثائق: رفع → تشفير → تخزين → وصول → حذف.
5. أسئلة لي — كل ما تحتاج قراره قبل الكود.

توقّف هنا. لا تبدأ الكود قبل موافقتي على الـ ERD والصلاحيات.

المرحلة 1 — البنية + تدفق الطالب والمختص (بعد موافقتي):

كل شي دفعة واحدة بلا توقّف:
- npx create-next-app + TypeScript strict + Tailwind + shadcn
- Prisma schema + migrations + seed (التصنيفات والمستويات)
- NextAuth (credentials + JWT + roles)
- Tenant middleware + audit log middleware
- .gitignore + .env.example + README
- صفحات الطالب: تسجيل، رفع وثائق، حالة الطلب
- صفحات المختص: قائمة المراجعة، شاشة التقييم والتصنيف، إنشاء
  خطة الدعم، اعتمادها
- صفحة الأستاذ: قائمة طلاب المقرر مع التسهيلات
- صفحة الإدارة: إدارة مستخدمين + أدوار
- API endpoints للتطبيق (القسم أعلاه)
- الهوية البصرية (كحلي/كريمي/برتقالي + Tajawal + RTL)

المرحلة 2 — اللوحات والتقارير والتجهيز:

- لوحة المختص: طلابه، استخدامهم، تنبيهاتهم، تقدّمهم
- لوحة الإدارة: إحصائيات الجامعة، تقارير قابلة للتصدير
- صفحة الطالب: تقدّمه المبسّط (بدون تفاصيل تصنيف)
- اختبارات الصلاحيات + عزل البيانات
- توثيق API

ابدأ بالمرحلة 0 — الآن. لا تكتب كوداً.
```
